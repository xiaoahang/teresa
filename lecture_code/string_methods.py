import sys, re

# with open('') as data:
#     for line in data:
#         print(line)

#  worth knowing that various  methods of  python string type are also useful for text manipulation
# # are simpler /  more efficient than regex
# # methods names provides a clear 'semantics' to code # 方法名让语义性更好

# 1. string tesing methods like
# # issupper()
# # islower()
# # isalpha()
# # isdigit()
# # isalnum()
# 2. upper(), lower(), isdigit(), isalumn()
# 3. splitting on a fixed string  (not regex): split
# # default 'splitting string' is space but can specify alternative as arg
"hello world".split()
"hello world".upper()
